import React from 'react';
import { ReactReduxContext } from 'react-redux';

function EditorOverTextArea(){
    return(<div>I am at editor over text area sample</div>);
}

export default EditorOverTextArea;